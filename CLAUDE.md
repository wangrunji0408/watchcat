# Claude Code Instructions

Follow the repository instructions in `AGENTS.md`. In particular, after every
completed change, run `pm2 restart watchcat` and verify the process with
`pm2 status watchcat`.

Do not copy real session data into tests.
