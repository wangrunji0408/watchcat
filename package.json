{
  "name": "watchcat",
  "version": "0.1.0",
  "description": "A LAN-accessible Agent session monitor",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "dependencies": {
    "marked": "15.0.12"
  },
  "engines": { "node": ">=18" }
}
